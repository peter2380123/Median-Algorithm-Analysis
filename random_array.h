#pragma once

// Fills an array with random numbers. Make sure you allocate this memory
// yourself beforehand!
//
// Inputs
// ------
// mem:
//  An array of integers to fill with random numbers.
//
// size:
//  Size of the array.
//
int *populate_random(int *mem, size_t size);
